# Database
# Required for both Node.js and Cloudflare Workers
DATABASE_URL="postgresql://username:password@localhost:5432/sporecmms"

# Prisma Accelerate URL (Required for Cloudflare Workers deployment)
# Get this from your Prisma Cloud dashboard after setting up Accelerate
# Format: https://xxxxxxxx.prisma-data.net/?api_key=xxxxxxxx
# ACCELERATE_URL=""

# Node environment
# Set to "development" for local dev, "production" for deployment
NODE_ENV="development"

# Session Secret (generate a secure random string)
# Used for session encryption and authentication
SESSION_SECRET="your-secret-key-here"

# Optional: Database connection pool size (Node.js only)
# Controls how many connections to the database are maintained
DATABASE_POOL_SIZE=10